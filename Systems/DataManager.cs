using System.Text.Json;

namespace DifferentWay.Systems;

public static class DataManager
{
    public static T LoadJson<T>(string filePath)
    {
        // Placeholder for loading JSON files into objects
        return default!;
    }
}
